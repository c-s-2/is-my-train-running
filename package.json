{
  "name": "is-my-train-ontime",
  "version": "1.0.0",
  "description": "Check if my train is on time",
  "main": "app.js",
  "scripts": {
    "dev": "node src/app.js && node src/dev.js",
    "build": "node src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Simpson",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "fs": "0.0.1-security",
    "node-fetch": "^2.6.0"
  }
}
